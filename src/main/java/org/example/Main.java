package org.example;

/**
 * Main — delegates to CalculatorApplication (Spring Boot entry point).
 * Run this class in IntelliJ to start the full-stack calculator server.
 */
public class Main {
    public static void main(String[] args) {
        CalculatorApplication.main(args);
    }
}